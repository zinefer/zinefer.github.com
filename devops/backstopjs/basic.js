const baseUrl = "http://host.docker.internal:1313";
const projectId = "portfolio";

const relativeUrls =[
  "/"
];

const viewports = [
  //"phone",
  //"tablet",
  "desktop",
];

module.exports = {
  baseUrl,
  projectId,
  relativeUrls,
  viewports,
};